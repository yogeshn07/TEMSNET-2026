# ============================================================
# TEMSNET-2026 Dependencies
# IEEE TEMSMET 2026 | XAI-NIDS Imbalance Study
# ============================================================
# Install: pip install -r requirements.txt
# ============================================================

# --- Core scientific stack ---
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1

# --- Parquet I/O (data/interim/ and data/processed/ artifacts) ---
# Declared explicitly rather than relying on it as an undeclared
# transitive dependency, for reproducibility (Research Task C.3).
pyarrow==17.0.0

# --- Machine learning ---
scikit-learn==1.5.1

# --- Model serialization (Research Task D.1) ---
# Declared explicitly rather than relying on it as an undeclared
# transitive dependency of scikit-learn.
joblib==1.5.2

# --- Class balancing (Research Task C.5) ---
imbalanced-learn==0.14.2

# --- Explainability ---
# shap version updated from the original scaffold pin (0.45.1) to the
# version actually installed and verified against the trained models
# (Research Task E.2).
shap==0.50.0
lime==0.2.0.1

# --- Visualization ---
matplotlib==3.9.1
seaborn==0.13.2

# --- Configuration ---
pyyaml==6.0.1
python-dotenv==1.0.1

# --- Logging ---
colorlog==6.8.2

# --- Testing ---
pytest==8.2.2
pytest-cov==5.0.0

# --- Code quality ---
ruff==0.5.5
